
//this object will hold the current username of who is logged in
const loginInfo = {
    //if a user is logged in then it will contain two properties:
    //username - the username of the user logged in
    //id - the id of the user logged in
}